startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({
      command: "npx",
      args: ["-y", "--package=fxgl-skills", "fxgl-skills-mcp"]
    })
