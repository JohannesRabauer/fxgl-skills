services:
  - type: web
    name: fxgl-skills-mcp
    runtime: node
    buildCommand: npm install
    startCommand: node mcp/server-http.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
