services:
  - type: web
    name: fxgl-skills-mcp
    runtime: node
    buildCommand: npm install
    startCommand: node mcp/server-http.js
    envVars:
      - key: NODE_ENV
        value: production
